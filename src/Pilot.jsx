import React, { Component } from 'react'

export default class Pilot extends Component {
    render() {
        return(
            <>
            <p>{this.props.name}</p>
            </>
        )
    }
}